#include "Employee.h"
#include <string>
using namespace std;

Employee::Employee(string name, int ID, string D, string P)
{
    name = name;
    idNumber = ID;
    department = D;
    position = P;
}
